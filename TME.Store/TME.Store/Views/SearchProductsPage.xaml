<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TME.Store.Views.SearchProductsPage"
             xmlns:views="clr-namespace:TME.Store.Views">


    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="45" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <SearchBar Grid.Row="0" Grid.Column="0" Placeholder="Szukaj produktu..."
                      x:Name="SearchBar" SearchCommand="{Binding SearchCommand}" 
                      SearchCommandParameter="{Binding Text, Source={x:Reference SearchBar}}"
                />

            <views:ProductListView  x:Name="ProductListView" Grid.Row="1" Grid.Column="0"/>

        </Grid>
    </ContentPage.Content>

    <!--<ContentPage.ToolbarItems>

        <ToolbarItem Name="logo" Order="Primary" Icon="logo_tme.png" Text="TME" Priority="0"/>
        <ToolbarItem  Order="Primary" Priority="0" />
        <ToolbarItem  Order="Primary" Priority="0" />
        <ToolbarItem  Order="Primary" Priority="0" />
        <ToolbarItem  Order="Primary" Priority="1" Icon="blue"/>

    </ContentPage.ToolbarItems>-->
</ContentPage>